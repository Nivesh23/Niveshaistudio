# Dependencies
node_modules/
.next/
.env
.env.local
.env.production

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Build output
dist/
build/
out/

# Large media files > 50MB (exceed GitHub 100MB limit — use cloud CDN instead)
app/public/hover2.mp4
app/public/media/

# These specific files are too large for GitHub (>25MB risk)
# app/Travel and tourism.mp4   — host on Cloudinary
# app/Finance.mp4              — host on Cloudinary

# Allow all other .mp4 videos (they are under 25MB and safe for GitHub)
